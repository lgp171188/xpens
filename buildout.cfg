[buildout]
parts = django
eggs =
     unipath
     psycopg2
     django-nvd3==0.7.4
     pillow
     django-simple-captcha

[versions]
django = 1.7.1

[django]
recipe = djangorecipe
eggs = ${buildout:eggs}
project = xpens
extra-paths = xpens
settings = settings
wsgi=true
